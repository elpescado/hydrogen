environment:
  QTDIR: "C:\\Qt\\5.11.1\\msvc2017_64"
  CMAKE_PREFIX_PATH: "C:\\Qt\\5.11.1\\msvc2017_64"
  VCPKG_DEFAULT_TRIPLET: "x64-windows"

image:
  - "Visual Studio 2017"

before_build:
  cmd: |-
      vcpkg install libarchive libsndfile portaudio cppunit
      vcpkg integrate install
      mkdir build
      cd build
      cmake --version
      cmake .. -G "Visual Studio 15 2017 Win64"

build:
  verbosity: detailed
